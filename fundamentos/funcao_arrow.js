const imprirSoma = function (a, b) {
    return a + b
}

const soma = (a, b) => {    //modo simples de criar uma função
    return a + b
}

//retorno implicito

const subtrai = (a, b) => a - b //quando o corpo da função só tem uma linha

const imprimi = a => console.log(a)

imprimi('bem bacana')